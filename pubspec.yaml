name: petzero
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  percent_indicator: ^4.2.2
  camera: ^0.11.1
  image_picker: ^1.0.2
  google_maps_flutter: ^2.6.0
  http: ^1.1.0
  path: ^1.8.3
  path_provider: ^2.1.1
  webview_flutter: ^4.4.2

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/aiImage/

  fonts:
    - family: SUIT
      fonts:
        - asset: assets/font/high1WonchuriTitleM.ttf



